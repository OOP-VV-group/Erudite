package servers;

import java.util.HashMap;

import chatBot.IOConsole;
import chatBot.IOModule;

public class VkServer {
	private HashMap<Integer, IOModule>modulesIO;
	/*
	private void closeIO(IOModule moduleIO) {
		this.erudite.closeIO(moduleIO.getId());
		moduleIO.close()
	}
	*/
	public static void main(String[] args) { 
	}
}
